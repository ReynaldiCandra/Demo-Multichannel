@import url('https://fonts.googleapis.com/css2?family=DM+Mono:wght@400;500&family=Manrope:wght@400;500;600;700;800&family=Space+Grotesk:wght@500;600;700&display=swap');
@import 'tailwindcss';
@import 'tw-animate-css';
@plugin "@tailwindcss/typography";

@custom-variant dark (&:is(.dark *));

@theme inline {
  --color-background: hsl(var(--background));
  --color-foreground: hsl(var(--foreground));
  --color-border: hsl(var(--border));
  --color-input: hsl(var(--input));
  --color-ring: hsl(var(--ring));

  --color-card: hsl(var(--card));
  --color-card-foreground: hsl(var(--card-foreground));
  --color-card-border: hsl(var(--card-border));

  --color-popover: hsl(var(--popover));
  --color-popover-foreground: hsl(var(--popover-foreground));
  --color-popover-border: hsl(var(--popover-border));

  --color-primary: hsl(var(--primary));
  --color-primary-foreground: hsl(var(--primary-foreground));
  --color-primary-border: var(--primary-border);

  --color-secondary: hsl(var(--secondary));
  --color-secondary-foreground: hsl(var(--secondary-foreground));
  --color-secondary-border: var(--secondary-border);

  --color-muted: hsl(var(--muted));
  --color-muted-foreground: hsl(var(--muted-foreground));
  --color-muted-border: var(--muted-border);

  --color-accent: hsl(var(--accent));
  --color-accent-foreground: hsl(var(--accent-foreground));
  --color-accent-border: var(--accent-border);

  --color-warning: hsl(var(--warning));
  --color-warning-foreground: hsl(var(--warning-foreground));

  --color-destructive: hsl(var(--destructive));
  --color-destructive-foreground: hsl(var(--destructive-foreground));
  --color-destructive-border: var(--destructive-border);

  --color-chart-1: hsl(var(--chart-1));
  --color-chart-2: hsl(var(--chart-2));
  --color-chart-3: hsl(var(--chart-3));
  --color-chart-4: hsl(var(--chart-4));
  --color-chart-5: hsl(var(--chart-5));

  --color-sidebar: hsl(var(--sidebar));
  --color-sidebar-foreground: hsl(var(--sidebar-foreground));
  --color-sidebar-border: hsl(var(--sidebar-border));
  --color-sidebar-primary: hsl(var(--sidebar-primary));
  --color-sidebar-primary-foreground: hsl(var(--sidebar-primary-foreground));
  --color-sidebar-primary-border: var(--sidebar-primary-border);
  --color-sidebar-accent: hsl(var(--sidebar-accent));
  --color-sidebar-accent-foreground: hsl(var(--sidebar-accent-foreground));
  --color-sidebar-accent-border: var(--sidebar-accent-border);
  --color-sidebar-ring: hsl(var(--sidebar-ring));

  --font-sans: var(--app-font-sans);
  --font-serif: var(--app-font-serif);
  --font-mono: var(--app-font-mono);

  --radius-sm: calc(var(--radius) - 4px);
  --radius-md: calc(var(--radius) - 2px);
  --radius-lg: var(--radius);
  --radius-xl: calc(var(--radius) + 4px);
}

/* LIGHT MODE */
:root {
  --button-outline: rgba(0, 0, 0, 0.1);
  --badge-outline: rgba(0, 0, 0, 0.05);

  /* Automatic computation of border around primary / danger buttons */
  --opaque-button-border-intensity: -8; /* In terms of percentages */

  /* Backgrounds applied on top of other backgrounds when hovered/active */
  --elevate-1: rgba(0, 0, 0, 0.03);
  --elevate-2: rgba(0, 0, 0, 0.08);

  --background: 210 32% 97%;
  --foreground: 222 32% 14%;
  --border: 214 24% 88%;
  --card: 0 0% 100%;
  --card-foreground: 222 32% 14%;
  --card-border: 214 24% 90%;
  --sidebar: 222 46% 13%;
  --sidebar-foreground: 210 30% 96%;
  --sidebar-border: 222 30% 26%;
  --sidebar-primary: 174 62% 42%;
  --sidebar-primary-foreground: 0 0% 100%;
  --sidebar-accent: 222 35% 20%;
  --sidebar-accent-foreground: 210 30% 96%;
  --sidebar-ring: 174 62% 42%;
  --popover: 0 0% 100%;
  --popover-foreground: 222 32% 14%;
  --popover-border: 214 24% 90%;
  --primary: 222 46% 13%;
  --primary-foreground: 0 0% 100%;
  --secondary: 210 25% 93%;
  --secondary-foreground: 222 32% 14%;
  --muted: 210 22% 94%;
  --muted-foreground: 222 15% 45%;
  --accent: 174 62% 42%;
  --accent-foreground: 0 0% 100%;
  --warning: 38 85% 55%;
  --warning-foreground: 32 55% 30%;
  --destructive: 4 69% 53%;
  --destructive-foreground: 42 40% 99%;
  --input: 214 22% 85%;
  --ring: 174 62% 42%;
  --chart-1: 174 62% 42%;
  --chart-2: 38 85% 60%;
  --chart-3: 222 40% 32%;
  --chart-4: 4 69% 53%;
  --chart-5: 30 76% 59%;
  --app-font-sans: 'Manrope', sans-serif;
  --app-font-serif: 'Space Grotesk', sans-serif;
  --app-font-mono: 'DM Mono', monospace;
  --radius: 0.8rem;
  --shadow-2xs: 0px 2px 0px 0px hsl(202.8169 89.1213% 53.1373% / 0); /*replace with H S L */
  --shadow-xs: 0px 2px 0px 0px hsl(202.8169 89.1213% 53.1373% / 0); /*replace with H S L */
  --shadow-sm:
    0px 2px 0px 0px hsl(202.8169 89.1213% 53.1373% / 0),
    0px 1px 2px -1px hsl(202.8169 89.1213% 53.1373% / 0);
  --shadow:
    0px 2px 0px 0px hsl(202.8169 89.1213% 53.1373% / 0),
    0px 1px 2px -1px hsl(202.8169 89.1213% 53.1373% / 0);
  --shadow-md:
    0px 2px 0px 0px hsl(202.8169 89.1213% 53.1373% / 0),
    0px 2px 4px -1px hsl(202.8169 89.1213% 53.1373% / 0);
  --shadow-lg:
    0px 2px 0px 0px hsl(202.8169 89.1213% 53.1373% / 0),
    0px 4px 6px -1px hsl(202.8169 89.1213% 53.1373% / 0);
  --shadow-xl:
    0px 2px 0px 0px hsl(202.8169 89.1213% 53.1373% / 0),
    0px 8px 10px -1px hsl(202.8169 89.1213% 53.1373% / 0);
  --shadow-2xl: 0px 2px 0px 0px hsl(202.8169 89.1213% 53.1373% / 0);
  --tracking-normal: 0em;
  --spacing: 0.25rem;

  /* Automatically computed borders - intensity can be controlled by the user by the --opaque-button-border-intensity setting */

  /* Fallback for older browsers */
  --sidebar-primary-border: hsl(var(--sidebar-primary));
  --sidebar-primary-border: hsl(
    from hsl(var(--sidebar-primary)) h s
      calc(l + var(--opaque-button-border-intensity)) / alpha
  );

  /* Fallback for older browsers */
  --sidebar-accent-border: hsl(var(--sidebar-accent));
  --sidebar-accent-border: hsl(
    from hsl(var(--sidebar-accent)) h s
      calc(l + var(--opaque-button-border-intensity)) / alpha
  );

  /* Fallback for older browsers */
  --primary-border: hsl(var(--primary));
  --primary-border: hsl(
    from hsl(var(--primary)) h s
      calc(l + var(--opaque-button-border-intensity)) / alpha
  );

  /* Fallback for older browsers */
  --secondary-border: hsl(var(--secondary));
  --secondary-border: hsl(
    from hsl(var(--secondary)) h s
      calc(l + var(--opaque-button-border-intensity)) / alpha
  );

  /* Fallback for older browsers */
  --muted-border: hsl(var(--muted));
  --muted-border: hsl(
    from hsl(var(--muted)) h s calc(l + var(--opaque-button-border-intensity)) /
      alpha
  );

  /* Fallback for older browsers */
  --accent-border: hsl(var(--accent));
  --accent-border: hsl(
    from hsl(var(--accent)) h s
      calc(l + var(--opaque-button-border-intensity)) / alpha
  );

  /* Fallback for older browsers */
  --destructive-border: hsl(var(--destructive));
  --destructive-border: hsl(
    from hsl(var(--destructive)) h s
      calc(l + var(--opaque-button-border-intensity)) / alpha
  );
}

.dark {
  --button-outline: rgba(255, 255, 255, 0.1);
  --badge-outline: rgba(255, 255, 255, 0.05);

  --opaque-button-border-intensity: 9; /* In terms of percentages */

  /* Backgrounds applied on top of other backgrounds when hovered/active */
  --elevate-1: rgba(255, 255, 255, 0.04);
  --elevate-2: rgba(255, 255, 255, 0.09);

  --background: 222 46% 10%;
  --foreground: 210 30% 96%;
  --border: 222 30% 22%;
  --card: 222 38% 14%;
  --card-foreground: 210 30% 96%;
  --card-border: 222 30% 22%;
  --sidebar: 224 48% 8%;
  --sidebar-foreground: 210 30% 96%;
  --sidebar-border: 222 30% 18%;
  --sidebar-primary: 174 62% 45%;
  --sidebar-primary-foreground: 0 0% 100%;
  --sidebar-accent: 222 32% 16%;
  --sidebar-accent-foreground: 210 30% 96%;
  --sidebar-ring: 174 62% 45%;
  --popover: 222 38% 14%;
  --popover-foreground: 210 30% 96%;
  --popover-border: 222 30% 22%;
  --primary: 174 62% 45%;
  --primary-foreground: 222 46% 10%;
  --secondary: 222 30% 18%;
  --secondary-foreground: 210 30% 96%;
  --muted: 222 28% 18%;
  --muted-foreground: 210 16% 70%;
  --accent: 174 62% 45%;
  --accent-foreground: 222 46% 10%;
  --warning: 38 85% 58%;
  --warning-foreground: 32 55% 16%;
  --destructive: 4 69% 53%;
  --destructive-foreground: 42 40% 99%;
  --input: 222 30% 24%;
  --ring: 174 62% 45%;
  --chart-1: 174 62% 45%;
  --chart-2: 38 85% 60%;
  --chart-3: 210 30% 96%;
  --chart-4: 4 69% 53%;
  --chart-5: 30 76% 59%;

  --shadow-2xs: 0px 2px 0px 0px hsl(202.8169 89.1213% 53.1373% / 0);
  --shadow-xs: 0px 2px 0px 0px hsl(202.8169 89.1213% 53.1373% / 0);
  --shadow-sm:
    0px 2px 0px 0px hsl(202.8169 89.1213% 53.1373% / 0),
    0px 1px 2px -1px hsl(202.8169 89.1213% 53.1373% / 0);
  --shadow:
    0px 2px 0px 0px hsl(202.8169 89.1213% 53.1373% / 0),
    0px 1px 2px -1px hsl(202.8169 89.1213% 53.1373% / 0);
  --shadow-md:
    0px 2px 0px 0px hsl(202.8169 89.1213% 53.1373% / 0),
    0px 2px 4px -1px hsl(202.8169 89.1213% 53.1373% / 0);
  --shadow-lg:
    0px 2px 0px 0px hsl(202.8169 89.1213% 53.1373% / 0),
    0px 4px 6px -1px hsl(202.8169 89.1213% 53.1373% / 0);
  --shadow-xl:
    0px 2px 0px 0px hsl(202.8169 89.1213% 53.1373% / 0),
    0px 8px 10px -1px hsl(202.8169 89.1213% 53.1373% / 0);
  --shadow-2xl: 0px 2px 0px 0px hsl(202.8169 89.1213% 53.1373% / 0);
}

@layer base {
  * {
    @apply border-border;
  }

  body {
    @apply font-sans antialiased bg-background text-foreground;
  }
}

/**
 * Using the elevate system.
 * Automatic contrast adjustment.
 *
 * <element className="hover-elevate" />
 * <element className="active-elevate-2" />
 *
 * // Using the tailwind utility when a data attribute is "on"
 * <element className="toggle-elevate data-[state=on]:toggle-elevated" />
 * // Or manually controlling the toggle state
 * <element className="toggle-elevate toggle-elevated" />
 *
 * Elevation systems have to handle many states.
 * - not-hovered, vs. hovered vs. active  (three mutually exclusive states)
 * - toggled or not
 * - focused or not (this is not handled with these utilities)
 *
 * Even without handling focused or not, this is six possible combinations that
 * need to be distinguished from eachother visually.
 */
@layer utilities {
  /* Hide ugly search cancel button in Chrome until we can style it properly */
  input[type='search']::-webkit-search-cancel-button {
    @apply hidden;
  }

  /* Placeholder styling for contentEditable div */
  [contenteditable][data-placeholder]:empty::before {
    content: attr(data-placeholder);
    color: hsl(var(--muted-foreground));
    pointer-events: none;
  }

  /* .no-default-hover-elevate/no-default-active-elevate is an escape hatch so consumers of
   * buttons/badges can remove the automatic brightness adjustment on interactions
   * and program their own. */
  .no-default-hover-elevate {
  }

  .no-default-active-elevate {
  }

  /**
   * Toggleable backgrounds go behind the content. Hoverable/active goes on top.
   * This way they can stack/compound. Both will overlap the parent's borders!
   * So borders will be automatically adjusted both on toggle, and hover/active,
   * and they will be compounded.
   */
  .toggle-elevate::before,
  .toggle-elevate-2::before {
    content: '';
    pointer-events: none;
    position: absolute;
    inset: 0px;
    /*border-radius: inherit;   match rounded corners */
    border-radius: inherit;
    z-index: -1;
    /* sits behind content but above backdrop */
  }

  .toggle-elevate.toggle-elevated::before {
    background-color: var(--elevate-2);
  }

  /* If there's a 1px border, adjust the inset so that it covers that parent's border */
  .border.toggle-elevate::before {
    inset: -1px;
  }

  /* Does not work on elements with overflow:hidden! */
  .hover-elevate:not(.no-default-hover-elevate),
  .active-elevate:not(.no-default-active-elevate),
  .hover-elevate-2:not(.no-default-hover-elevate),
  .active-elevate-2:not(.no-default-active-elevate) {
    position: relative;
    z-index: 0;
  }

  .hover-elevate:not(.no-default-hover-elevate)::after,
  .active-elevate:not(.no-default-active-elevate)::after,
  .hover-elevate-2:not(.no-default-hover-elevate)::after,
  .active-elevate-2:not(.no-default-active-elevate)::after {
    content: '';
    pointer-events: none;
    position: absolute;
    inset: 0px;
    /*border-radius: inherit;   match rounded corners */
    border-radius: inherit;
    z-index: 999;
    /* sits in front of content */
  }

  .hover-elevate:hover:not(.no-default-hover-elevate)::after,
  .active-elevate:active:not(.no-default-active-elevate)::after {
    background-color: var(--elevate-1);
  }

  .hover-elevate-2:hover:not(.no-default-hover-elevate)::after,
  .active-elevate-2:active:not(.no-default-active-elevate)::after {
    background-color: var(--elevate-2);
  }

  /* If there's a 1px border, adjust the inset so that it covers that parent's border */
  .border.hover-elevate:not(.no-hover-interaction-elevate)::after,
  .border.active-elevate:not(.no-active-interaction-elevate)::after,
  .border.hover-elevate-2:not(.no-hover-interaction-elevate)::after,
  .border.active-elevate-2:not(.no-active-interaction-elevate)::after,
  .border.hover-elevate:not(.no-hover-interaction-elevate)::after {
    inset: -1px;
  }
}

@layer base {
  html { background: hsl(var(--background)); }
  body { min-width: 320px; }
  button, input, select, textarea { font: inherit; }
  button, a { -webkit-tap-highlight-color: transparent; }
}

:root { color-scheme: light; }
* { box-sizing: border-box; }
body { position: relative; background: linear-gradient(180deg, hsl(210 40% 98%), hsl(210 34% 95%)); }
body::before, body::after { content: ''; position: fixed; z-index: 0; border-radius: 50%; filter: blur(90px); pointer-events: none; }
body::before { width: 520px; height: 520px; top: -160px; right: -120px; background: hsl(174 65% 55% / .16); }
body::after { width: 480px; height: 480px; bottom: -180px; left: -80px; background: hsl(222 55% 30% / .1); }
.app-shell { min-height: 100dvh; display: flex; position: relative; z-index: 1; background: transparent; color: hsl(var(--foreground)); }
.sidebar { position: relative; width: 238px; background: linear-gradient(165deg, hsl(var(--sidebar) / .94), hsl(222 48% 8% / .9)); backdrop-filter: blur(22px) saturate(160%); -webkit-backdrop-filter: blur(22px) saturate(160%); color: hsl(var(--sidebar-foreground)); display: flex; flex-direction: column; padding: 26px 14px 18px; flex-shrink: 0; border-right: 1px solid hsl(var(--sidebar-border) / .5); transition: width .22s ease; }
.brand { min-height: 42px; display: flex; align-items: center; justify-content: space-between; gap: 10px; padding: 0 5px 34px; }
.brand-logo { display: block; object-fit: contain; object-position: left center; }
.brand-logo-full { width: 156px; height: auto; }
.brand-logo-mark { display: none; width: 36px; height: 36px; }
.brand strong { display: block; font: 700 20px var(--app-font-serif); letter-spacing: -.04em; }
.brand small, .operator small { display: block; color: hsl(var(--sidebar-foreground) / .55); font-size: 10px; letter-spacing: .04em; margin-top: 1px; }
.sidebar-toggle { width: 29px; height: 29px; display: grid; place-items: center; flex-shrink: 0; border: 1px solid hsl(var(--sidebar-foreground) / .15); border-radius: 7px; background: transparent; color: hsl(var(--sidebar-foreground) / .72); cursor: pointer; transition: background .18s ease, color .18s ease, border-color .18s ease; }
.sidebar-toggle:hover { background: hsl(var(--sidebar-accent)); color: hsl(var(--sidebar-foreground)); border-color: hsl(var(--sidebar-foreground) / .28); }
.sidebar-menu { display: grid; gap: 20px; min-height: 0; overflow-y: auto; scrollbar-width: thin; scrollbar-color: hsl(var(--sidebar-foreground) / .2) transparent; }
.sidebar-group { display: grid; gap: 0; }
.sidebar-kicker { color: hsl(var(--sidebar-foreground) / .42); font: 500 9px var(--app-font-mono); letter-spacing: .14em; padding: 0 12px 10px; }
.pos-kicker { margin-top: 25px; }
.side-nav { display: grid; gap: 3px; }
.side-link { color: hsl(var(--sidebar-foreground) / .66); display: flex; align-items: center; gap: 11px; min-height: 39px; padding: 0 12px; border-radius: 8px; text-decoration: none; font-size: 12px; font-weight: 700; transition: background .18s ease, color .18s ease, transform .18s ease; }
.side-link:hover { color: hsl(var(--sidebar-foreground)); background: hsl(var(--sidebar-accent)); transform: translateX(2px); }
.side-link.active { background: hsl(var(--sidebar-primary)); color: hsl(var(--sidebar-primary-foreground)); }
.side-link svg { flex-shrink: 0; opacity: .86; }
.nav-badge { margin-left: auto; min-width: 17px; height: 17px; padding: 0 5px; border-radius: 999px; display: inline-grid; place-items: center; background: hsl(270 66% 89%); color: hsl(270 47% 51%); font: 700 9px var(--app-font-mono); }
.side-link.active .nav-badge { background: hsl(var(--sidebar-primary-foreground) / .15); color: inherit; }
.nav-dot { margin-left: auto; width: 6px; height: 6px; border-radius: 50%; background: hsl(4 69% 61%); }
.sub-link-mark { width: 5px; height: 5px; border-radius: 50%; background: currentColor; opacity: .7; margin-left: 2px; }
.sidebar-bottom { margin-top: auto; display: grid; gap: 15px; }
.operator { border-top: 1px solid hsl(var(--sidebar-border)); padding: 16px 10px 0; display: flex; gap: 9px; align-items: center; }
.operator-copy { min-width: 0; }
.operator strong { font-size: 11px; }
.avatar { width: 29px; height: 29px; border-radius: 50%; display: grid; place-items: center; background: hsl(var(--accent)); color: hsl(var(--accent-foreground)); font: 700 10px var(--app-font-mono); flex-shrink: 0; }
.avatar-teal { background: hsl(169 43% 47%); color: hsl(42 40% 99%); }
.main-area { flex: 1; min-width: 0; }
.sidebar-collapsed { width: 76px; padding-left: 12px; padding-right: 12px; }
.sidebar-collapsed .brand { justify-content: center; padding-left: 0; padding-right: 0; }
.sidebar-collapsed .brand-logo-full, .sidebar-collapsed .sidebar-kicker, .sidebar-collapsed .side-link span, .sidebar-collapsed .operator-copy { display: none; }
.sidebar-collapsed .brand-logo-mark { display: block; }
.sidebar-collapsed .sidebar-toggle { position: absolute; top: 31px; right: -13px; z-index: 2; width: 27px; height: 27px; border-color: hsl(var(--border)); background: hsl(var(--background)); color: hsl(var(--foreground)); box-shadow: 0 2px 8px hsl(231 28% 16% / .12); }
.sidebar-collapsed .sidebar-menu { gap: 16px; }
.sidebar-collapsed .side-nav { gap: 6px; }
.sidebar-collapsed .side-link { justify-content: center; padding: 0; }
.sidebar-collapsed .nav-badge { display: none; }
.sidebar-collapsed .operator { justify-content: center; padding-left: 0; padding-right: 0; }
.topbar { position: sticky; top: 0; z-index: 4; height: 65px; display: flex; align-items: center; justify-content: space-between; padding: 0 34px; border-bottom: 1px solid hsl(var(--border) / .6); background: hsl(var(--background) / .55); backdrop-filter: blur(18px) saturate(160%); -webkit-backdrop-filter: blur(18px) saturate(160%); }
.crumb { display: flex; align-items: center; gap: 7px; color: hsl(var(--muted-foreground)); font-size: 11px; }
.crumb strong { color: hsl(var(--foreground)); font-size: 12px; }
.topbar-right { display: flex; align-items: center; gap: 18px; color: hsl(var(--muted-foreground)); font-size: 10px; }
.live-pulse { display: inline-flex; align-items: center; gap: 6px; color: hsl(169 43% 37%); font: 500 10px var(--app-font-mono); }
.live-pulse i { width: 6px; height: 6px; border-radius: 50%; background: hsl(169 43% 47%); box-shadow: 0 0 0 3px hsl(169 43% 47% / .14); }
.today { font-size: 11px; }
.content { max-width: 1400px; margin: 0 auto; padding: 38px 42px 72px; }
.page-title { display: flex; align-items: flex-end; justify-content: space-between; gap: 20px; margin-bottom: 30px; }
.eyebrow { color: hsl(var(--muted-foreground)); font: 500 9px var(--app-font-mono); letter-spacing: .14em; text-transform: uppercase; margin-bottom: 8px; }
h1, h2, h3, p { margin: 0; }
h1 { font: 700 clamp(26px, 3vw, 37px)/1.1 var(--app-font-serif); letter-spacing: -.045em; }
h2 { font: 700 16px/1.2 var(--app-font-serif); letter-spacing: -.02em; }
.page-title p { color: hsl(var(--muted-foreground)); font-size: 12px; margin-top: 9px; max-width: 500px; }
.panel { background: hsl(var(--card) / .68); border: 1px solid hsl(var(--card-border) / .55); border-radius: 16px; backdrop-filter: blur(18px) saturate(140%); -webkit-backdrop-filter: blur(18px) saturate(140%); box-shadow: 0 10px 30px hsl(222 45% 15% / .06), inset 0 1px 0 hsl(0 0% 100% / .5); }
.kpi-grid { display: grid; grid-template-columns: repeat(4, minmax(0, 1fr)); gap: 12px; margin-bottom: 18px; }
.kpi-card { padding: 17px 18px 16px; border-top: 3px solid hsl(var(--primary)); min-height: 130px; }
.accent-yellow { border-top-color: hsl(var(--warning)); }
.accent-teal { border-top-color: hsl(169 43% 47%); }
.accent-orange { border-top-color: hsl(30 76% 59%); }
.accent-red { border-top-color: hsl(var(--destructive)); }
.kpi-top { display: flex; align-items: center; justify-content: space-between; color: hsl(var(--muted-foreground)); font-size: 11px; font-weight: 700; }
.kpi-icon { width: 29px; height: 29px; display: grid; place-items: center; border-radius: 8px; background: hsl(var(--secondary)); color: hsl(var(--primary)); }
.accent-yellow .kpi-icon { background: hsl(var(--warning) / .22); color: hsl(var(--warning-foreground)); }
.accent-teal .kpi-icon { background: hsl(169 43% 47% / .13); color: hsl(169 43% 37%); }
.accent-orange .kpi-icon { background: hsl(30 76% 59% / .14); color: hsl(30 62% 42%); }
.accent-red .kpi-icon { background: hsl(var(--destructive) / .12); color: hsl(var(--destructive)); }
.kpi-value { display: block; font: 700 22px var(--app-font-mono); letter-spacing: -.06em; margin: 14px 0 7px; }
.kpi-meta { display: flex; align-items: center; gap: 3px; color: hsl(var(--muted-foreground)); font-size: 10px; }
.positive { color: hsl(169 43% 37%); }.negative { color: hsl(var(--destructive)); }
.dashboard-grid { display: grid; grid-template-columns: minmax(0, 1.2fr) minmax(310px, .8fr); gap: 18px; margin-bottom: 18px; }
.trend-panel, .attention-panel, .recent-panel, .table-panel { padding: 22px; }
.supplier-panel { overflow: hidden; }
.supplier-panel > .section-head { margin: 0; padding: 18px 22px 15px; border-bottom: 1px solid hsl(var(--border) / .72); }
.supplier-state { padding: 8px 22px 18px; }
.supplier-state .state { min-height: 128px; gap: 7px; }
.supplier-state > .state > svg { width: 28px; height: 28px; padding: 7px; border-radius: 10px; color: hsl(var(--primary)); background: hsl(var(--secondary)); box-sizing: content-box; }
.supplier-state .state strong { font-size: 12px; }
.supplier-state .state span { max-width: 260px; }
.supplier-state .state .btn { min-height: 32px; }
.section-head { display: flex; align-items: flex-start; justify-content: space-between; gap: 12px; margin-bottom: 22px; }
.text-link, .table-link, .back-link { color: hsl(169 43% 37%); text-decoration: none; font-size: 11px; font-weight: 800; display: inline-flex; align-items: center; gap: 3px; }
.text-link:hover, .table-link:hover, .back-link:hover { text-decoration: underline; }
.chart-wrap { height: 235px; position: relative; padding: 8px 5px 0; }
.chart-svg { width: 100%; height: 196px; overflow: visible; background: repeating-linear-gradient(to bottom, transparent 0, transparent 48px, hsl(var(--border) / .62) 49px, transparent 50px); }
.chart-labels { display: flex; justify-content: space-between; padding-top: 10px; color: hsl(var(--muted-foreground)); font: 10px var(--app-font-mono); }
.attention-list { display: grid; gap: 3px; }
.attention-item { display: flex; align-items: center; gap: 10px; padding: 10px 8px; text-decoration: none; color: inherit; border-radius: 8px; transition: background .18s ease; }
.attention-item:hover { background: hsl(var(--secondary)); }
.attention-mark { display: grid; place-items: center; width: 28px; height: 28px; background: hsl(30 76% 59% / .16); color: hsl(30 62% 42%); border-radius: 7px; }
.attention-item div { flex: 1; min-width: 0; }.attention-item strong, .attention-item small { display: block; }.attention-item strong { font-size: 12px; }.attention-item small { color: hsl(var(--muted-foreground)); font-size: 10px; margin-top: 3px; }.attention-amount { font: 500 10px var(--app-font-mono); color: hsl(var(--muted-foreground)); }
.recent-panel { min-height: 240px; }
.filter-row { display: flex; align-items: center; justify-content: space-between; margin: -7px 0 15px; }
.tabs { display: flex; gap: 4px; }.tab { border: 0; padding: 8px 12px; background: hsl(var(--secondary)); color: hsl(var(--foreground)); border-radius: 7px; font-size: 11px; font-weight: 700; }
.month-input { border: 1px solid hsl(var(--input)); background: hsl(var(--card)); color: hsl(var(--foreground)); border-radius: 7px; padding: 9px 10px; font-size: 11px; }
.btn { display: inline-flex; align-items: center; justify-content: center; gap: 7px; min-height: 36px; border: 1px solid transparent; border-radius: 7px; padding: 0 13px; cursor: pointer; font-size: 11px; font-weight: 800; transition: transform .18s ease, background .18s ease, border-color .18s ease; }
.btn:hover { transform: translateY(-1px); }.btn:disabled { cursor: wait; opacity: .6; transform: none; }
.btn-primary { background: hsl(var(--primary)); color: hsl(var(--primary-foreground)); }.btn-primary:hover { background: hsl(222 42% 22%); }
.btn-secondary { background: hsl(var(--secondary)); color: hsl(var(--secondary-foreground)); border-color: hsl(var(--border)); }.btn-secondary:hover { background: hsl(210 25% 88%); }
.btn-ghost { background: transparent; color: hsl(var(--muted-foreground)); }.btn-danger { background: hsl(var(--destructive)); color: hsl(var(--destructive-foreground)); }
.badge { display: inline-flex; align-items: center; width: fit-content; border-radius: 99px; padding: 4px 7px; font: 500 9px var(--app-font-mono); white-space: nowrap; }.badge-neutral { color: hsl(var(--muted-foreground)); background: hsl(var(--secondary)); }.badge-good { color: hsl(169 43% 32%); background: hsl(169 43% 47% / .13); }.badge-warn, .badge-yellow { color: hsl(var(--warning-foreground)); background: hsl(var(--warning) / .28); }.badge-danger { color: hsl(4 60% 41%); background: hsl(var(--destructive) / .13); }
.table-scroll { overflow-x: auto; }.table-scroll table { width: 100%; border-collapse: collapse; min-width: 640px; }.table-scroll th { color: hsl(var(--muted-foreground)); font: 500 9px var(--app-font-mono); letter-spacing: .06em; text-transform: uppercase; text-align: left; padding: 0 12px 12px; border-bottom: 1px solid hsl(var(--border)); white-space: nowrap; }.table-scroll td { padding: 14px 12px; border-bottom: 1px solid hsl(var(--border) / .7); font-size: 11px; white-space: nowrap; }.table-scroll tr:last-child td { border-bottom: 0; }.table-scroll td strong { font-size: 11px; }.right { text-align: right !important; }.muted { color: hsl(var(--muted-foreground)); }.mono { font-family: var(--app-font-mono); font-size: 10px !important; }.profit-text { color: hsl(169 43% 37%); }.warn-text { color: hsl(30 62% 42%); }.table-sub { display: block; color: hsl(var(--muted-foreground)); font-size: 10px; margin-top: 4px; }.row-alert td:first-child { border-left: 2px solid hsl(var(--destructive)); }.overdue { color: hsl(var(--destructive)); display: inline-flex; gap: 4px; align-items: center; }.icon-btn { width: 30px; height: 30px; display: inline-grid; place-items: center; border: 0; border-radius: 6px; background: transparent; color: hsl(var(--muted-foreground)); cursor: pointer; }.icon-btn:hover { background: hsl(var(--secondary)); color: hsl(var(--foreground)); }
.product-thumb { width: 42px; height: 42px; display: block; object-fit: cover; border-radius: 8px; border: 1px solid hsl(var(--border)); background: hsl(var(--secondary)); }.product-thumb-small { width: 34px; height: 34px; }.product-thumb-empty { display: grid; place-items: center; color: hsl(var(--muted-foreground)); font-size: 14px; }.product-preview { width: 72px; height: 72px; object-fit: cover; border-radius: 9px; border: 1px solid hsl(var(--border)); }.image-preview-button { position: relative; display: inline-flex; padding: 0; border: 0; border-radius: 8px; background: transparent; cursor: pointer; }.image-preview-button:hover .image-eye, .image-preview-button:focus-visible .image-eye { opacity: 1; }.image-preview-button .product-thumb { transition: filter .18s ease; }.image-preview-button:hover .product-thumb { filter: brightness(.76); }.image-eye { position: absolute; inset: 0; display: grid; place-items: center; color: white; background: hsl(222 46% 13% / .44); border-radius: 8px; opacity: .72; transition: opacity .18s ease; }.image-preview-button-small .product-thumb { width: 34px; height: 34px; }.image-lightbox { display: block; width: min(100%, 520px); max-height: min(65dvh, 520px); object-fit: contain; margin: 0 auto; border-radius: 12px; background: hsl(var(--secondary)); }.supplier-product-list { display: grid; gap: 8px; min-width: 220px; white-space: normal; }.supplier-product { display: flex; align-items: center; gap: 8px; }.supplier-product-copy { display: grid; gap: 3px; min-width: 0; }.supplier-product-copy strong { overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }.supplier-product-copy small { color: hsl(var(--muted-foreground)); font-size: 9px; }
.danger-icon { color: hsl(var(--destructive)); }.danger-icon:hover { color: hsl(var(--destructive)); background: hsl(var(--destructive) / .12); }.table-actions { justify-content: flex-end; gap: 2px; }
.state { min-height: 160px; display: flex; flex-direction: column; justify-content: center; align-items: center; gap: 8px; color: hsl(var(--muted-foreground)); text-align: center; font-size: 11px; }.state strong { color: hsl(var(--foreground)); font-size: 12px; }.state span { max-width: 230px; line-height: 1.5; }.state .btn { margin-top: 5px; }.state-icon { width: 27px; height: 27px; border-radius: 8px; }.state-line { width: 120px; height: 9px; border-radius: 3px; }.state-line.short { width: 80px; }.skeleton { background: linear-gradient(90deg, hsl(var(--secondary)), hsl(var(--card)), hsl(var(--secondary))); background-size: 200% 100%; animation: shimmer 1.4s ease infinite; }.kpi-skeleton { height: 130px; border-radius: 12px; border: 1px solid hsl(var(--border)); } @keyframes shimmer { from { background-position: 200% 0; } to { background-position: -200% 0; } }
.experiment-strip { display: flex; align-items: center; justify-content: space-between; gap: 20px; padding: 18px 21px; margin-bottom: 15px; background: hsl(var(--primary)); color: hsl(var(--primary-foreground)); border-color: hsl(var(--primary)); }.experiment-strip .eyebrow { color: hsl(var(--primary-foreground) / .6); }.experiment-strip strong { display: block; font: 600 15px var(--app-font-serif); }.strip-stats { display: flex; gap: 28px; font-size: 10px; color: hsl(var(--primary-foreground) / .65); }.strip-stats b { display: block; color: hsl(var(--accent)); font: 500 16px var(--app-font-mono); margin-bottom: 3px; }
.job-overview { display: flex; gap: 34px; margin: -5px 0 20px; }.job-overview div { display: flex; flex-direction: column; gap: 4px; }.job-overview span { color: hsl(var(--muted-foreground)); font-size: 10px; }.job-overview strong { font: 600 22px var(--app-font-mono); }.job-overview .warn strong { color: hsl(30 62% 42%); }
.button-pair { display: flex; gap: 8px; align-items: center; }.back-link { margin-bottom: 19px; color: hsl(var(--muted-foreground)); }.detail-kpis { display: grid; grid-template-columns: repeat(4, 1fr); gap: 12px; margin-bottom: 18px; }.detail-grid, .ledger-columns { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 18px; }.detail-grid > .panel, .ledger-columns > .panel { padding: 21px; }.ledger-list { display: grid; gap: 1px; }.ledger-row, .ledger-metric { display: flex; justify-content: space-between; align-items: center; padding: 13px 0; border-bottom: 1px solid hsl(var(--border)); }.ledger-row:last-child, .ledger-metric:last-child { border-bottom: 0; }.ledger-row span { display: grid; gap: 4px; }.ledger-row small { color: hsl(var(--muted-foreground)); font-size: 10px; }.ledger-row b { font-size: 11px !important; }
.ledger-hero { display: flex; justify-content: space-between; align-items: flex-end; background: hsl(var(--primary)); color: hsl(var(--primary-foreground)); border-radius: 12px; padding: 25px 27px; margin-bottom: 18px; }.ledger-hero .eyebrow { color: hsl(var(--primary-foreground) / .6); }.ledger-hero strong { display: block; color: hsl(var(--accent)); font: 600 clamp(27px, 5vw, 43px) var(--app-font-mono); letter-spacing: -.08em; }.ledger-hero > div > span:last-child { display: block; color: hsl(var(--primary-foreground) / .63); font-size: 11px; margin-top: 5px; }.ledger-hero-side { text-align: right; color: hsl(var(--primary-foreground) / .65); font-size: 11px; }.ledger-hero-side b { display: block; color: hsl(var(--primary-foreground)); font: 500 15px var(--app-font-mono); margin-top: 5px; }.ledger-metric strong, .ledger-metric.strong { border-color: hsl(var(--accent) / .4); }.ledger-metric.strong b { color: hsl(169 43% 37%); }.ledger-history { margin-top: 18px; padding: 21px; }.month-list { display: grid; grid-template-columns: repeat(3, 1fr); gap: 8px; }.month-row { display: flex; align-items: center; gap: 8px; justify-content: space-between; padding: 13px 12px; border: 1px solid hsl(var(--border)); background: transparent; color: hsl(var(--foreground)); border-radius: 7px; cursor: pointer; font-size: 10px; }.month-row.selected { background: hsl(var(--secondary)); border-color: hsl(var(--accent)); }.month-row b { margin-left: auto; }.settings-placeholder { max-width: 550px; padding: 42px; text-align: center; }.settings-symbol { width: 50px; height: 50px; display: grid; place-items: center; background: hsl(var(--accent) / .25); color: hsl(40 73% 38%); margin: 0 auto 18px; border-radius: 13px; }.settings-placeholder p { color: hsl(var(--muted-foreground)); font-size: 12px; line-height: 1.7; margin: 11px auto 22px; max-width: 410px; }.settings-note { display: inline-flex; align-items: center; gap: 7px; color: hsl(169 43% 37%); background: hsl(169 43% 47% / .1); padding: 10px 12px; border-radius: 7px; font-size: 10px; font-weight: 700; }
.host-list { display: grid; gap: 4px; }.host-row { display: flex; align-items: center; gap: 10px; padding: 9px 0; }.host-row > div { flex: 1; }.host-row strong, .host-row small { display: block; }.host-row small { color: hsl(var(--muted-foreground)); font-size: 10px; margin-top: 3px; }.live-grid { display: grid; grid-template-columns: .72fr 1.28fr; gap: 18px; }.live-grid > .panel { padding: 21px; }
.mini-action { border: 1px solid hsl(var(--border)); background: hsl(var(--secondary)); color: hsl(var(--foreground)); border-radius: 5px; padding: 5px 7px; cursor: pointer; font-size: 9px; font-weight: 800; }.mini-action:hover { border-color: hsl(var(--accent)); }.mini-action:disabled { opacity: .55; cursor: wait; }
.modal-backdrop { position: fixed; z-index: 20; inset: 0; display: grid; place-items: center; padding: 20px; background: hsl(231 28% 16% / .48); backdrop-filter: blur(6px); -webkit-backdrop-filter: blur(6px); animation: fade-in .18s ease; }.modal { width: min(100%, 610px); max-height: calc(100dvh - 40px); overflow-y: auto; background: linear-gradient(145deg, hsl(var(--card) / .88), hsl(210 32% 97% / .72)); border: 1px solid hsl(0 0% 100% / .68); border-radius: 20px; backdrop-filter: blur(28px) saturate(160%); -webkit-backdrop-filter: blur(28px) saturate(160%); box-shadow: 0 24px 70px hsl(222 45% 13% / .3), inset 0 1px 0 hsl(0 0% 100% / .7); padding: 25px; animation: rise-in .22s ease; }.modal-head { display: flex; justify-content: space-between; align-items: flex-start; margin-bottom: 22px; }.modal h2 { font-size: 20px; }.form-grid { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 14px 12px; align-items: start; }.field { display: grid; gap: 7px; min-width: 0; }.field > span { font-size: 10px; font-weight: 800; color: hsl(var(--muted-foreground)); }.field small { color: hsl(var(--muted-foreground)); font-size: 9px; line-height: 1.45; }.field:last-of-type:has(textarea), .form-grid .form-actions { grid-column: 1 / -1; }.field input, .field select, .field textarea { width: 100%; background: hsl(var(--background) / .66); color: hsl(var(--foreground)); border: 1px solid hsl(var(--input) / .9); border-radius: 9px; min-height: 39px; padding: 9px 11px; outline: none; font-size: 11px; transition: border-color .18s ease, box-shadow .18s ease, background .18s ease; }.field input[type="file"] { padding: 8px; }.field input:focus, .field select:focus, .field textarea:focus { border-color: hsl(var(--accent)); background: hsl(var(--card) / .8); box-shadow: 0 0 0 3px hsl(var(--accent) / .18); }.form-actions { display: flex; justify-content: flex-end; gap: 8px; padding-top: 9px; border-top: 1px solid hsl(var(--border) / .7); margin-top: 2px; }.pagination { display: flex; justify-content: center; align-items: center; gap: 5px; margin-top: 17px; padding-top: 15px; border-top: 1px solid hsl(var(--border) / .65); }.pagination-btn { min-width: 30px; height: 30px; display: inline-grid; place-items: center; border: 1px solid hsl(var(--border)); border-radius: 7px; background: hsl(var(--card) / .66); color: hsl(var(--foreground)); cursor: pointer; font-size: 10px; font-weight: 800; }.pagination-btn:hover:not(:disabled), .pagination-btn.active { background: hsl(var(--primary)); border-color: hsl(var(--primary)); color: hsl(var(--primary-foreground)); }.pagination-btn:disabled { opacity: .4; cursor: not-allowed; }.mobile-menu, .mobile-scrim { display: none; } @keyframes fade-in { from { opacity: 0; } to { opacity: 1; } } @keyframes rise-in { from { opacity: 0; transform: translateY(8px); } to { opacity: 1; transform: translateY(0); } }
@media (max-width: 980px) { .sidebar { width: 210px; }.sidebar.sidebar-collapsed { width: 76px; }.content { padding: 30px 26px 60px; }.topbar { padding: 0 26px; }.kpi-grid, .detail-kpis { grid-template-columns: repeat(2, minmax(0, 1fr)); }.dashboard-grid, .live-grid { grid-template-columns: 1fr; } }
@media (max-width: 700px) { .sidebar { position: fixed; z-index: 30; inset: 0 auto 0 0; width: 246px; transform: translateX(-105%); transition: transform .22s ease; box-shadow: 16px 0 35px hsl(231 28% 16% / .2); }.sidebar-open { transform: translateX(0); }.mobile-scrim { display: block; position: fixed; z-index: 25; inset: 0; border: 0; background: hsl(231 28% 16% / .3); }.mobile-menu { display: grid; place-items: center; border: 0; background: transparent; color: hsl(var(--foreground)); padding: 5px; }.topbar { height: 57px; padding: 0 16px; }.crumb { margin-left: 5px; }.topbar-tools { gap: 8px; }.workspace-search { display: none; }.account-copy, .account-chevron { display: none; }.account-control { padding: 0; border: 0; background: transparent; }.topbar-right { display: none; }.content { padding: 26px 15px 48px; }.page-title { align-items: flex-start; flex-direction: column; gap: 16px; margin-bottom: 24px; }.page-title > .btn, .page-title > .button-pair { width: 100%; }.page-title > .btn { justify-content: center; }.button-pair { width: 100%; }.button-pair > * { flex: 1; }.kpi-grid, .detail-kpis { grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 8px; }.kpi-card { min-height: 119px; padding: 13px; }.kpi-value { font-size: 16px; margin-top: 12px; }.kpi-meta { font-size: 9px; }.kpi-icon { width: 25px; height: 25px; }.dashboard-grid, .dashboard-lower-grid, .dashboard-bottom-grid { gap: 10px; grid-template-columns: 1fr; }.trend-panel, .attention-panel, .recent-panel, .table-panel, .marketplace-panel, .store-performance-panel, .upcoming-panel { padding: 16px; }.chart-wrap { height: 190px; }.chart-svg { height: 150px; }.experiment-strip { align-items: flex-start; flex-direction: column; padding: 16px; }.strip-stats { gap: 22px; }.filter-row { align-items: stretch; gap: 8px; flex-direction: column; }.month-input { width: 100%; }.job-overview { gap: 20px; overflow-x: auto; }.job-overview strong { font-size: 18px; }.detail-grid, .ledger-columns { grid-template-columns: 1fr; gap: 10px; }.ledger-hero { align-items: flex-start; flex-direction: column; gap: 17px; padding: 20px; }.ledger-hero-side { text-align: left; }.month-list { grid-template-columns: 1fr; }.form-grid { grid-template-columns: 1fr; }.field:last-of-type:has(textarea), .form-grid .form-actions { grid-column: auto; }.modal { padding: 18px; }.settings-placeholder { padding: 28px 18px; }.today { display: none; } }

/* Dashboard cockpit extensions */
.topbar-tools { display: flex; align-items: center; gap: 20px; }
.workspace-search { width: min(255px, 24vw); height: 34px; display: flex; align-items: center; gap: 8px; border: 1px solid hsl(var(--border) / .7); border-radius: 7px; padding: 0 10px; background: hsl(var(--card) / .55); backdrop-filter: blur(10px); color: hsl(var(--muted-foreground)); transition: border-color .18s ease, background .18s ease; }
.workspace-search:focus-within { border-color: hsl(var(--accent)); background: hsl(var(--card)); }
.workspace-search input { min-width: 0; width: 100%; border: 0; outline: 0; background: transparent; color: hsl(var(--foreground)); font-size: 10px; }
.workspace-search input::placeholder { color: hsl(var(--muted-foreground)); }
.account-wrap { position: relative; }
.account-control { display: flex; align-items: center; gap: 8px; border: 0; background: transparent; color: hsl(var(--foreground)); cursor: pointer; padding: 2px 0; }
.account-control .avatar { width: 27px; height: 27px; font-size: 9px; }
.account-copy { display: grid; gap: 2px; text-align: left; }
.account-copy strong { font-size: 10px; }
.account-copy small { color: hsl(var(--muted-foreground)); font-size: 9px; }
.account-chevron { color: hsl(var(--muted-foreground)); transform: rotate(90deg); transition: transform .18s ease; }
.account-chevron-open { transform: rotate(-90deg); }
.account-menu { position: absolute; z-index: 12; top: calc(100% + 12px); right: 0; width: 190px; display: grid; gap: 5px; padding: 13px; background: hsl(var(--card) / .88); border: 1px solid hsl(var(--border) / .6); border-radius: 12px; backdrop-filter: blur(22px) saturate(150%); -webkit-backdrop-filter: blur(22px) saturate(150%); box-shadow: 0 14px 30px hsl(222 45% 13% / .16); animation: rise-in .18s ease; }
.account-menu strong { font-size: 11px; }
.account-menu span { color: hsl(var(--muted-foreground)); font-size: 9px; }
.account-menu a { color: hsl(169 43% 37%); text-decoration: none; font-size: 10px; font-weight: 800; margin-top: 6px; }
.section-caption { color: hsl(var(--muted-foreground)); font-size: 10px; margin-top: 6px; }
.dashboard-grid { grid-template-columns: minmax(0, 1.12fr) minmax(300px, .88fr); }
.marketplace-panel { padding: 22px; }
.marketplace-list { display: grid; gap: 2px; }
.marketplace-row { display: flex; align-items: center; gap: 10px; min-height: 53px; border-bottom: 1px solid hsl(var(--border) / .7); }
.marketplace-row:last-child { border-bottom: 0; }
.marketplace-avatar { width: 29px; height: 29px; display: grid; place-items: center; border-radius: 8px; color: hsl(var(--primary-foreground)); font: 700 11px var(--app-font-serif); }
.marketplace-avatar-0 { background: hsl(169 43% 42%); }.marketplace-avatar-1 { background: hsl(30 76% 55%); }.marketplace-avatar-2 { background: hsl(232 34% 35%); }.marketplace-avatar-3 { background: hsl(4 55% 55%); }
.marketplace-name { min-width: 0; flex: 1; }.marketplace-name strong, .marketplace-name small { display: block; }.marketplace-name strong { overflow: hidden; text-overflow: ellipsis; font-size: 11px; }.marketplace-name small { color: hsl(var(--muted-foreground)); font-size: 9px; margin-top: 3px; }
.marketplace-share { text-align: right; }.marketplace-share strong, .marketplace-share span { display: block; }.marketplace-share strong { font: 600 12px var(--app-font-mono); }.marketplace-share span { color: hsl(var(--muted-foreground)); font: 9px var(--app-font-mono); margin-top: 3px; }
.dashboard-lower-grid, .dashboard-bottom-grid { display: grid; grid-template-columns: minmax(0, 1.12fr) minmax(300px, .88fr); gap: 18px; margin-bottom: 18px; }
.store-performance-panel, .upcoming-panel { padding: 22px; }
.store-performance-list { display: grid; gap: 15px; }
.store-performance-row { display: grid; grid-template-columns: minmax(150px, 1fr) minmax(80px, 1.25fr) auto; align-items: center; gap: 14px; }
.store-performance-title { display: flex; align-items: center; gap: 10px; min-width: 0; }.store-performance-title > div { min-width: 0; }.store-performance-title strong, .store-performance-title small { display: block; }.store-performance-title strong { overflow: hidden; text-overflow: ellipsis; font-size: 11px; }.store-performance-title small { color: hsl(var(--muted-foreground)); font-size: 9px; margin-top: 3px; }.store-rank { color: hsl(var(--muted-foreground)); font: 500 10px var(--app-font-mono); }
.performance-bar { height: 6px; overflow: hidden; background: hsl(var(--secondary)); border-radius: 99px; }.performance-bar span { display: block; height: 100%; border-radius: inherit; background: hsl(var(--accent)); transition: width .35s ease; }
.task-list { display: grid; gap: 2px; }.task-row { display: flex; align-items: center; gap: 10px; padding: 9px 0; color: inherit; text-decoration: none; border-bottom: 1px solid hsl(var(--border) / .7); }.task-row:last-child { border-bottom: 0; }.task-row:hover .task-copy strong { color: hsl(169 43% 37%); }.task-date { width: 42px; height: 35px; display: grid; place-items: center; flex-shrink: 0; background: hsl(var(--secondary)); border-radius: 6px; color: hsl(var(--primary)); font: 500 9px var(--app-font-mono); text-align: center; }.task-date-alert { background: hsl(var(--destructive) / .12); color: hsl(var(--destructive)); }.task-copy { min-width: 0; flex: 1; }.task-copy strong, .task-copy small { display: block; }.task-copy strong { font-size: 11px; }.task-copy small { color: hsl(var(--muted-foreground)); font-size: 9px; margin-top: 3px; }
.dashboard-bottom-grid { grid-template-columns: minmax(0, 1.12fr) minmax(300px, .88fr); margin-bottom: 0; }.activity-list { display: grid; gap: 2px; }.activity-row { display: flex; align-items: center; gap: 10px; padding: 9px 0; border-bottom: 1px solid hsl(var(--border) / .7); }.activity-row:last-child { border-bottom: 0; }.activity-icon { width: 29px; height: 29px; display: grid; place-items: center; flex-shrink: 0; border-radius: 8px; background: hsl(169 43% 47% / .13); color: hsl(169 43% 37%); }.activity-row > div:nth-child(2) { min-width: 0; flex: 1; }.activity-row strong, .activity-row small { display: block; }.activity-row strong { overflow: hidden; text-overflow: ellipsis; font-size: 11px; }.activity-row small { color: hsl(var(--muted-foreground)); font-size: 9px; margin-top: 3px; }.activity-value { text-align: right; }.activity-value strong { font: 500 10px var(--app-font-mono); }.activity-value small { font: 9px var(--app-font-mono); }
.insight-card { position: relative; overflow: hidden; min-height: 238px; padding: 25px 24px; background: hsl(var(--primary)); color: hsl(var(--primary-foreground)); border-color: hsl(var(--primary)); }.insight-card .eyebrow { position: relative; color: hsl(var(--primary-foreground) / .55); }.insight-card h2 { position: relative; max-width: 250px; color: hsl(var(--primary-foreground)); font-size: 21px; line-height: 1.15; margin: 34px 0 10px; }.insight-card p { position: relative; max-width: 280px; color: hsl(var(--primary-foreground) / .65); font-size: 10px; line-height: 1.6; }.insight-link { position: relative; display: inline-flex; align-items: center; gap: 4px; margin-top: 20px; color: hsl(var(--accent)); text-decoration: none; font-size: 10px; font-weight: 800; }.insight-link:hover { text-decoration: underline; }.insight-orbit { position: absolute; border: 1px solid hsl(var(--accent) / .28); border-radius: 50%; pointer-events: none; }.insight-orbit-one { width: 230px; height: 230px; right: -72px; top: -56px; }.insight-orbit-two { width: 150px; height: 150px; right: -31px; top: -15px; border-color: hsl(169 43% 55% / .32); }
@media (max-width: 700px) { .topbar-tools { gap: 8px; }.workspace-search { display: none; }.account-copy, .account-chevron { display: none; }.account-control { padding: 0; border: 0; background: transparent; }.dashboard-grid, .dashboard-lower-grid, .dashboard-bottom-grid { grid-template-columns: 1fr; gap: 10px; }.store-performance-row { grid-template-columns: minmax(125px, 1fr) minmax(70px, 1fr) auto; gap: 9px; }.insight-card { min-height: 215px; } }

/* ===== Hero banner ===== */
.hero-banner { position: relative; overflow: hidden; border-radius: 20px; padding: 30px 32px; margin-bottom: 20px; background: linear-gradient(135deg, hsl(222 48% 14%) 0%, hsl(205 46% 24%) 48%, hsl(174 62% 38%) 100%); color: hsl(0 0% 100%); box-shadow: 0 18px 45px hsl(222 45% 13% / .25); }
.hero-banner::before { content: ''; position: absolute; inset: 0; background: radial-gradient(circle at 88% 12%, hsl(0 0% 100% / .16), transparent 55%); pointer-events: none; }
.hero-banner-top { position: relative; z-index: 1; display: flex; align-items: flex-start; justify-content: space-between; gap: 20px; flex-wrap: wrap; }
.hero-banner .eyebrow { color: hsl(0 0% 100% / .65); }
.hero-banner h1 { color: hsl(0 0% 100%); }
.hero-banner p { color: hsl(0 0% 100% / .74); margin-top: 8px; max-width: 460px; font-size: 12px; }
.hero-banner-stats { position: relative; z-index: 1; display: flex; gap: 14px; margin-top: 24px; flex-wrap: wrap; }
.hero-stat { display: flex; flex-direction: column; gap: 4px; padding: 12px 16px; border-radius: 12px; background: hsl(0 0% 100% / .12); backdrop-filter: blur(6px); border: 1px solid hsl(0 0% 100% / .18); min-width: 128px; }
.hero-stat span { font-size: 10px; color: hsl(0 0% 100% / .68); }
.hero-stat strong { font: 700 17px var(--app-font-mono); letter-spacing: -.03em; }
@media (max-width: 700px) { .hero-banner { padding: 20px; } .hero-banner-stats { gap: 10px; } .hero-stat { min-width: 104px; padding: 10px 12px; } }

/* ===== Halaman login ===== */
.login-shell { position: relative; overflow: hidden; min-height: 100dvh; display: grid; place-items: center; padding: 24px; background: linear-gradient(135deg, hsl(222 48% 11%) 0%, hsl(205 46% 20%) 48%, hsl(174 62% 32%) 100%); }
.login-shell::before, .login-shell::after { content: ''; position: absolute; z-index: 0; border-radius: 50%; filter: blur(80px); pointer-events: none; }
.login-shell::before { width: 460px; height: 460px; top: -140px; left: -110px; background: hsl(174 70% 55% / .45); }
.login-shell::after { width: 420px; height: 420px; bottom: -160px; right: -90px; background: hsl(46 90% 60% / .3); }
.login-card { position: relative; z-index: 1; width: min(100%, 400px); background: hsl(0 0% 100% / .1); border: 1px solid hsl(0 0% 100% / .22); backdrop-filter: blur(24px) saturate(160%); -webkit-backdrop-filter: blur(24px) saturate(160%); border-radius: 20px; box-shadow: 0 28px 70px hsl(222 45% 8% / .45); padding: 36px 32px 32px; color: hsl(0 0% 100%); }
.login-brand { display: flex; justify-content: center; margin-bottom: 28px; }
.login-brand img { display: block; width: min(100%, 300px); height: auto; }
.login-card .eyebrow { color: hsl(0 0% 100% / .62); }
.login-card h1 { color: hsl(0 0% 100%); font: 700 26px/1.1 var(--app-font-serif); letter-spacing: -.04em; }
.login-sub { color: hsl(0 0% 100% / .74); font-size: 11px; line-height: 1.6; margin-top: 9px; }
.login-form { display: grid; gap: 14px; margin-top: 24px; }
.login-card .field > span { color: hsl(0 0% 100% / .8); }
.login-card .field input { background: hsl(0 0% 100% / .1); border-color: hsl(0 0% 100% / .28); color: hsl(0 0% 100%); }
.login-card .field input::placeholder { color: hsl(0 0% 100% / .45); }
.login-card .field input:focus { border-color: hsl(174 70% 62%); box-shadow: 0 0 0 3px hsl(174 70% 62% / .28); }
.password-field { position: relative; }
.password-field input { padding-right: 38px; }
.password-toggle { position: absolute; right: 6px; top: 50%; transform: translateY(-50%); width: 28px; height: 28px; display: grid; place-items: center; border: 0; background: transparent; color: hsl(var(--muted-foreground)); cursor: pointer; border-radius: 6px; }
.password-toggle:hover { background: hsl(var(--secondary)); color: hsl(var(--foreground)); }
.login-card .password-toggle { color: hsl(0 0% 100% / .68); }
.login-card .password-toggle:hover { background: hsl(0 0% 100% / .14); color: hsl(0 0% 100%); }
.login-forgot { display: flex; justify-content: flex-end; margin-top: -6px; }
.login-forgot button { background: none; border: 0; padding: 0; color: hsl(174 75% 68%); font-size: 10px; font-weight: 700; cursor: pointer; }
.login-forgot button:hover { text-decoration: underline; }
.login-submit { width: 100%; margin-top: 4px; }
.login-error { display: flex; align-items: center; gap: 7px; color: hsl(0 0% 100%); background: hsl(4 69% 53% / .3); border: 1px solid hsl(4 69% 53% / .4); border-radius: 7px; padding: 10px 11px; font-size: 10px; font-weight: 700; }
.login-hint { margin-top: 20px; padding-top: 16px; border-top: 1px solid hsl(0 0% 100% / .18); color: hsl(0 0% 100% / .65); font-size: 10px; line-height: 1.7; }
.login-hint b { color: hsl(0 0% 100%); font-family: var(--app-font-mono); }
.account-logout { display: inline-flex; align-items: center; gap: 6px; margin-top: 4px; padding: 7px 0 0; border: 0; border-top: 1px solid hsl(var(--border)); background: transparent; color: hsl(4 60% 45%); cursor: pointer; font-size: 10px; font-weight: 800; text-align: left; }
.account-logout:hover { text-decoration: underline; }
.demo-banner { display: flex; align-items: center; gap: 8px; margin-bottom: 18px; padding: 10px 13px; border-radius: 8px; background: hsl(var(--warning) / .22); color: hsl(var(--warning-foreground)); font-size: 10px; font-weight: 700; }

.settings-intro { display: flex; align-items: center; gap: 16px; padding: 20px 22px; margin-bottom: 16px; }.settings-intro h2 { margin-top: 4px; font-size: 17px; }.settings-intro p { color: hsl(var(--muted-foreground)); font-size: 11px; line-height: 1.55; margin-top: 5px; max-width: 590px; }.settings-intro .settings-note { margin-left: auto; white-space: nowrap; }.settings-grid { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 13px; }.module-card { padding: 19px 20px; }.module-card-head { display: flex; align-items: flex-start; justify-content: space-between; gap: 12px; }.module-card h2 { margin-top: 4px; font-size: 16px; }.module-kicker { color: hsl(var(--muted-foreground)); font: 500 9px var(--app-font-mono); letter-spacing: .06em; text-transform: uppercase; }.module-card > p { min-height: 34px; color: hsl(var(--muted-foreground)); font-size: 10px; line-height: 1.55; margin-top: 12px; }.module-card-foot { display: flex; align-items: center; justify-content: space-between; gap: 12px; border-top: 1px solid hsl(var(--border)); margin-top: 16px; padding-top: 13px; }.module-state, .module-locked { display: inline-flex; align-items: center; gap: 5px; color: hsl(var(--muted-foreground)); font-size: 9px; }.module-locked { color: hsl(40 73% 38%); }.module-toggle { display: inline-flex; align-items: center; gap: 6px; border: 1px solid hsl(var(--border)); border-radius: 6px; padding: 7px 9px; background: hsl(var(--secondary)); color: hsl(var(--foreground)); cursor: pointer; font-size: 9px; font-weight: 800; }.module-toggle:hover:not(:disabled) { border-color: hsl(var(--accent)); }.module-toggle:disabled { cursor: not-allowed; opacity: .5; }

/* ── Analisa toko ─────────────────────────────────────────────────────── */
.pill-row { display: flex; flex-wrap: wrap; gap: 6px; margin: -6px 0 14px; align-items: center; }
.pill-row-sub { margin-top: -6px; }
.pill-label { color: hsl(var(--muted-foreground)); font: 500 9px var(--app-font-mono); letter-spacing: .06em; text-transform: uppercase; margin-right: 2px; }
.pill { border: 1px solid hsl(var(--border)); background: hsl(var(--card)); color: hsl(var(--foreground)); border-radius: 99px; padding: 8px 14px; font-size: 11px; font-weight: 700; cursor: pointer; transition: background .15s ease, color .15s ease; }
.pill:hover { background: hsl(var(--secondary)); }
.pill.active { background: hsl(var(--primary)); border-color: hsl(var(--primary)); color: hsl(var(--primary-foreground)); }
.pill-sm { padding: 5px 11px; font-size: 10px; }
.tab { cursor: pointer; }
.tab.active { background: hsl(var(--primary)); color: hsl(var(--primary-foreground)); }
.tabs-sort { margin: -8px 0 16px; flex-wrap: wrap; }
.report-body { transition: opacity .15s ease; }
.report-body.is-stale { opacity: .55; pointer-events: none; }
.kpi-grid-compact .kpi-card { min-height: 104px; }
.kpi-grid-compact .kpi-value { font-size: 17px; }
.marketplace-button { width: 100%; border: 0; background: transparent; text-align: left; cursor: pointer; font: inherit; color: inherit; border-radius: 8px; }
.marketplace-button:hover { background: hsl(var(--secondary)); }
.daily-bars { display: flex; align-items: flex-end; gap: 4px; height: 190px; padding-top: 8px; overflow-x: auto; }
.daily-bar { flex: 1 0 14px; max-width: 34px; height: 100%; display: flex; flex-direction: column; justify-content: flex-end; align-items: center; gap: 6px; }
.daily-bar span { display: block; width: 100%; background: hsl(var(--accent)); border-radius: 4px 4px 0 0; min-height: 3px; }
.daily-bar:hover span { background: hsl(40 73% 45%); }
.daily-bar small { color: hsl(var(--muted-foreground)); font: 9px var(--app-font-mono); }
.search-box { display: flex; align-items: center; gap: 7px; border: 1px solid hsl(var(--input)); border-radius: 7px; padding: 0 10px; background: hsl(var(--card)); color: hsl(var(--muted-foreground)); }
.search-box input { border: 0; outline: 0; background: transparent; padding: 9px 0; font-size: 11px; color: hsl(var(--foreground)); width: 150px; }
.share-cell { display: flex; align-items: center; gap: 8px; min-width: 110px; }
.share-cell .performance-bar { flex: 1; }
.show-more { margin: 14px auto 0; display: block; border: 0; background: transparent; color: hsl(169 43% 37%); font-size: 11px; font-weight: 800; cursor: pointer; }
.show-more:hover { text-decoration: underline; }
@media (max-width: 700px) { .kpi-grid-compact { grid-template-columns: repeat(2, minmax(0, 1fr)); } .search-box input { width: 110px; } }

.kpi-link { display: block; text-decoration: none; color: inherit; }
.kpi-link .kpi-card { height: 100%; transition: transform .18s ease; }
.kpi-link:hover .kpi-card { transform: translateY(-2px); }
.kpi-cta-text { font-size: 15px !important; }
.store-link { text-decoration: none; color: inherit; border-radius: 8px; }
.store-link:hover .store-performance-title strong { color: hsl(169 43% 37%); }

/* ── POS: status, edit, filter ───────────────────────────────────────── */
.filter-row-wrap { flex-wrap: wrap; gap: 10px; }
.row-muted td { opacity: .55; }
.row-muted td:last-child, .row-muted td:nth-last-child(2) { opacity: 1; }
.status-cell { display: flex; align-items: center; gap: 6px; }
.status-select { border: 1px solid hsl(var(--input)); background: hsl(var(--card)); color: hsl(var(--muted-foreground)); border-radius: 6px; font-size: 10px; padding: 3px 4px; cursor: pointer; }
.form-inline-link { grid-column: 1 / -1; text-align: left; margin: -6px 0 0; }
.report-note { display: flex; flex-wrap: wrap; gap: 8px 16px; align-items: center; padding: 11px 14px; margin-bottom: 18px; border: 1px dashed hsl(var(--border)); border-radius: 10px; font-size: 11px; color: hsl(var(--muted-foreground)); }
.report-note b { color: hsl(var(--foreground)); font-family: var(--app-font-mono); font-weight: 600; }
.dashboard-lower-last { margin-bottom: 0; }

/* ---------- RealtimeInsightBanner ---------- */
/* Dua kartu banner berdampingan dengan gap yang jelas: kiri = insight
   (bisa diklik), kanan = sumber data real-time (statis). */
.rt-insight-grid { display: grid; grid-template-columns: minmax(0, 1.05fr) minmax(0, 1fr); align-items: stretch; gap: 20px; margin-bottom: 20px; }
.rt-card { position: relative; overflow: hidden; display: flex; flex-direction: column; padding: 24px 28px; border-radius: 20px; color: hsl(0 0% 100%); box-shadow: 0 18px 45px hsl(222 45% 13% / .18); }
.rt-card::before { content: ''; position: absolute; inset: 0; background: radial-gradient(circle at 92% 8%, hsl(0 0% 100% / .12), transparent 52%); pointer-events: none; }
.rt-card > * { position: relative; z-index: 1; }
.rt-card-full { grid-column: 1 / -1; }

/* Kiri: bisa diklik (seluruh kartu adalah <Link>). */
.rt-card-insight { background: linear-gradient(150deg, hsl(212 62% 13%) 0%, hsl(203 55% 21%) 100%); text-decoration: none; cursor: pointer; transition: transform .15s ease, box-shadow .15s ease; }
.rt-card-insight:hover { transform: translateY(-2px); box-shadow: 0 22px 50px hsl(222 45% 13% / .3); }
.rt-card-insight:focus-visible { outline: 2px solid hsl(172 78% 68%); outline-offset: 2px; }

/* Kanan: statis, tidak bisa diklik. */
.rt-card-source { background: linear-gradient(120deg, hsl(196 55% 23%) 0%, hsl(172 56% 31%) 100%); cursor: default; }

.rt-main { display: flex; align-items: flex-start; justify-content: space-between; gap: 16px; flex: 1; min-width: 0; }
.rt-main-copy { display: flex; flex-direction: column; align-items: flex-start; min-width: 0; padding-left: 22px; }

/* Icon kecil + label, ditarik ke kiri supaya teksnya SEJAJAR persis dengan
   judul dan deskripsi di bawahnya (bukan icon yang menggeser teks label). */
.rt-lead { display: flex; align-items: center; gap: 6px; margin-left: -22px; color: hsl(0 0% 100% / .78); font-size: 12px; font-weight: 600; }
.rt-icon { width: 22px; height: 22px; display: grid; place-items: center; border-radius: 7px; background: hsl(172 60% 42% / .3); color: hsl(172 80% 74%); flex-shrink: 0; }
.rt-insight-grid h2 { margin: 14px 0 0; color: hsl(0 0% 100%); font: 700 clamp(18px, 1.9vw, 24px)/1.2 var(--app-font-serif); letter-spacing: -.03em; }
.rt-insight-grid p { margin: 8px 0 0; max-width: 460px; color: hsl(0 0% 100% / .72); font-size: 12px; line-height: 1.65; }
.rt-link { display: inline-flex; align-items: center; gap: 4px; margin-top: auto; padding-top: 14px; color: hsl(172 78% 68%); font-size: 11px; font-weight: 700; text-decoration: none; background: none; border: 0; cursor: pointer; }
.rt-card-insight:hover .rt-link { text-decoration: underline; }
.rt-retry { margin-top: 14px; padding-top: 0; cursor: pointer; }

/* Badge kanal unggulan di pojok kanan kartu kiri. */
.rt-leader { display: flex; flex-direction: column; align-items: center; gap: 8px; flex-shrink: 0; }
.rt-leader-badge { width: 46px; height: 46px; border-radius: 13px; font-size: 15px; }
.rt-leader-growth { display: inline-flex; align-items: center; gap: 2px; font: 700 13px var(--app-font-mono); color: hsl(160 78% 62%); white-space: nowrap; }

.rt-source-head { display: flex; flex-direction: column; gap: 4px; margin-bottom: 16px; }
.rt-source-title { display: flex; align-items: center; gap: 8px; color: hsl(0 0% 100%); font-size: 13px; font-weight: 700; }
.rt-source-updated { font-size: 11px; color: hsl(0 0% 100% / .78); }
.rt-dot { width: 8px; height: 8px; border-radius: 50%; background: hsl(160 75% 55%); box-shadow: 0 0 0 3px hsl(160 75% 55% / .22); flex-shrink: 0; }

/* Satu baris sejajar, bukan grid 2x2. */
.rt-tiles { display: flex; gap: 10px; flex-wrap: wrap; }
.rt-tile { flex: 1 1 110px; min-width: 100px; display: flex; align-items: center; gap: 10px; padding: 10px 12px; border-radius: 12px; background: hsl(0 0% 100% / .1); border: 1px solid hsl(0 0% 100% / .16); min-width: 0; }
.rt-tile-copy { display: flex; flex-direction: column; gap: 3px; min-width: 0; }
.rt-avatar { width: 32px; height: 32px; border-radius: 9px; font-size: 12px; flex-shrink: 0; }
.rt-tile-name { max-width: 100%; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; color: hsl(0 0% 100% / .82); font-size: 11px; }
.rt-growth { display: inline-flex; align-items: center; gap: 2px; font: 700 14px var(--app-font-mono); letter-spacing: -.04em; white-space: nowrap; }
.rt-up { color: hsl(160 78% 62%); }
.rt-down { color: hsl(4 92% 76%); }
.rt-flat, .rt-new { color: hsl(0 0% 100% / .72); }
.rt-foot { display: flex; align-items: center; gap: 5px; margin-top: 14px; color: hsl(0 0% 100% / .55); font-size: 9.5px; line-height: 1.5; }
.rt-empty { color: hsl(0 0% 100% / .62); font-size: 11px; line-height: 1.6; }
.rt-insight-loading { min-height: 138px; justify-content: center; gap: 10px; }
.rt-skel { border-radius: 8px; background: linear-gradient(90deg, hsl(0 0% 100% / .1), hsl(0 0% 100% / .2), hsl(0 0% 100% / .1)); background-size: 200% 100%; }
.rt-skel-title { width: min(280px, 70%); height: 24px; }
.rt-skel-line { width: min(380px, 85%); height: 12px; }
@media (max-width: 980px) { .rt-insight-grid { grid-template-columns: 1fr; gap: 16px; } .rt-card { padding: 20px 22px; } .rt-tiles { flex-wrap: wrap; } .rt-tile { flex: 1 1 45%; } }
@media (max-width: 700px) { .rt-card { padding: 18px; border-radius: 16px; } .rt-tile { flex: 1 1 100%; } }

/* Dashboard dan master data memakai ritme tipografi yang lebih rapat dan seragam. */
.dashboard-greeting { margin-bottom: 18px; }
.dashboard-greeting h1 { margin-top: 5px; font-size: 23px; letter-spacing: -.035em; }
.dashboard-greeting p { color: hsl(var(--muted-foreground)); font-size: 11px; margin-top: 5px; }
.rt-insight-grid { grid-auto-rows: minmax(202px, 1fr); }
.rt-card { min-height: 202px; padding: 20px 22px; border-radius: 16px; }
.rt-lead, .rt-source-title { font-size: 11px; }
.rt-insight-grid h2 { margin-top: 11px; font-size: 17px; }
.rt-insight-grid p { font-size: 11px; line-height: 1.55; }
.rt-link { font-size: 10px; padding-top: 10px; }
.rt-source-head { margin-bottom: 12px; }
.rt-source-updated, .rt-tile-name { font-size: 10px; }
.rt-growth { font-size: 12px; }
.section-head h2 { font-size: 15px; }
.section-caption { font-size: 10px; }
.page-title { margin-bottom: 22px; }
.page-title h1 { font-size: 23px; }
.page-title p { font-size: 11px; margin-top: 6px; }
.panel { border-radius: 13px; }
.kpi-card { padding: 16px 17px; }
.kpi-value { font-size: 20px; margin: 11px 0 6px; }
.kpi-top { font-size: 10px; }
.kpi-meta { font-size: 9px; }
.modal { padding: 20px; border-radius: 15px; }
.modal-head { margin-bottom: 18px; }
.modal h2 { font-size: 17px; }
.field { gap: 6px; }
.field > span { font-size: 9px; }
.field input, .field select, .field textarea { min-height: 35px; padding: 7px 9px; font-size: 10px; }
.field small { font-size: 8px; }
.master-toolbar { margin-bottom: 12px; }
.sort-control { display: inline-flex; align-items: center; gap: 7px; color: hsl(var(--muted-foreground)); font-size: 10px; font-weight: 700; }
.sort-control select { border: 1px solid hsl(var(--input)); border-radius: 7px; padding: 8px 10px; background: hsl(var(--card)); color: hsl(var(--foreground)); font-size: 10px; }
.top-products-card { margin-top: 18px; padding: 20px; }
.top-product-list { display: grid; gap: 2px; }
.top-product-row { display: flex; align-items: center; gap: 11px; padding: 10px 0; border-bottom: 1px solid hsl(var(--border) / .7); }
.top-product-row:last-child { border-bottom: 0; }
.top-product-copy { min-width: 0; flex: 1; }
.top-product-copy strong, .top-product-copy small, .top-product-value strong, .top-product-value small { display: block; }
.top-product-copy strong { font-size: 11px; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.top-product-copy small, .top-product-value small { color: hsl(var(--muted-foreground)); font-size: 9px; margin-top: 3px; }
.top-product-value { text-align: right; }
.top-product-value strong { font: 500 10px var(--app-font-mono); }
